# 마음담음 — 기술 스택 선정 이유 및 역할 발표 대본

---

## 인트로

저희 팀이 만든 "마음담음"은 결혼, 부고, 승진 같은 경조사 상황에서 관계와 상황, 톤을 고르면 AI가 봉투 문구·문자·카카오톡 메시지 세 가지 안을 지어주는 서비스입니다.

이 서비스를 만들면서 저희가 기술 스택을 고를 때 기준으로 삼은 건 딱 하나였습니다. **"로그인 없이도 전체 기능이 완전히 동작해야 한다"**는 것, 그리고 **"경조사라는 격식 있는 상황에 어울리는 결과물을 안정적으로 내야 한다"**는 것입니다. 지금부터 이 두 기준을 중심으로, 저희가 왜 이 스택들을 선택했고 각각이 어떤 역할을 맡고 있는지 설명드리겠습니다.

---

## 1. 프론트엔드 — React 19 + TypeScript + Vite 6

먼저 프론트엔드는 **React 19와 TypeScript, 그리고 빌드 도구로 Vite 6**을 사용했습니다.

관계 프로필, 경조사 카테고리, 세부 키워드, 커스텀 프롬프트, 형식 선택까지 — 저희 서비스는 사용자가 여러 단계를 거쳐 입력한 값들이 서로 맞물려서 최종 프롬프트를 만들어내는 구조입니다. 이렇게 상태(state)가 여러 컴포넌트에 걸쳐 복잡하게 얽히는 화면일수록, 컴포넌트 단위로 상태를 관리하고 재사용할 수 있는 React가 유리하다고 판단했습니다.

TypeScript는 필수로 가져갔습니다. 관계 데이터, 키워드 데이터, AI가 반환하는 메시지 후보 데이터까지 — 타입이 어긋나면 바로 화면이 깨지거나 잘못된 문구가 나갈 수 있는 서비스이기 때문에, 컴파일 단계에서 타입 오류를 잡아내는 게 실수를 줄이는 데 큰 도움이 됐습니다.

Vite는 개발 서버 속도가 빠르다는 것도 있지만, 결정적으로 **Express 서버와 미들웨어 모드로 통합**할 수 있다는 점이 컸습니다. 뒤에서 다시 설명드리겠지만, 저희는 프론트엔드 개발 서버와 백엔드 API 서버를 굳이 따로 띄우지 않고 하나의 Express 프로세스 안에서 같이 돌아가게 구성했습니다.

---

## 2. 스타일링 — Tailwind CSS v4

디자인 시스템은 **Tailwind CSS v4**로 구현했습니다.

경조사 서비스다 보니 "신뢰감 있고 격식 있어 보이는가"가 저희에게는 굉장히 중요한 기준이었습니다. 그래서 초기엔 그냥 눈에 보이는 대로 빠르게 스타일을 붙였는데, 오히려 그게 문제였습니다 — 색상 코드가 파일마다 흩어져 있고, 그림자·테두리·여백이 제각각이라 "AI가 대충 만든 것 같다"는 피드백을 계속 받았습니다.

그래서 Tailwind의 유틸리티 클래스 체계를 활용해서 잉크색(#111827), 배경색(#F9FAFB), 포인트 컬러(앰버) 세 가지로 컬러 토큰을 확정하고, `index.css`의 테마 변수 하나만 바꾸면 폰트 전체가 한 번에 바뀌는 구조로 정리했습니다. 유틸리티 클래스 기반이라 컴포넌트 파일 안에서 바로 스타일을 확인하고 고칠 수 있다는 점도, 여러 라운드에 걸쳐 디자인을 계속 다듬어야 했던 저희 작업 방식과 잘 맞았습니다.

---

## 3. 백엔드 — Express

백엔드는 **Express**를 그대로 사용했습니다.

저희 서비스의 백엔드가 하는 일은 사실 단순합니다. 프론트엔드에서 관계·상황·톤 정보를 받아서, Gemini API에 보낼 프롬프트를 조립하고, 그 결과를 다시 JSON으로 돌려주는 API 엔드포인트 하나가 핵심입니다. 이렇게 역할이 명확한 단일 API 서버에는 Express처럼 가볍고 설정이 단순한 프레임워크가 오히려 적합하다고 판단했습니다.

그리고 앞서 말씀드린 것처럼, 개발 중에는 이 Express 서버가 Vite를 미들웨어로 물어서 프론트엔드 정적 파일까지 같이 서빙하고, 배포 시에는 esbuild로 번들링된 서버 하나만 실행하면 되도록 구성했습니다. 서버를 여러 개 따로 관리할 필요가 없어서, 배포 과정 자체가 단순해진다는 실질적인 이점이 있었습니다.

---

## 4. 핵심 — Google Gemini API (`@google/genai`)

이 서비스의 심장은 **Google Gemini API**입니다. `@google/genai` SDK를 통해 서버에서 직접 호출합니다.

여기서 저희가 신경 쓴 부분이 두 가지입니다.

**첫째, 프롬프트를 그냥 사용자가 입력한 문장 그대로 보내지 않았습니다.** 관계 정보(호칭, 관계 유형, 친밀도 점수, 선호 톤), 선택된 경조사 상황과 세부 키워드, 그 상황에 맞는 금기어·예법 주의사항, 그리고 문구 형식별 작성 규칙(봉투 문구는 10~25자 이내 한자 표기 위주로, 카톡 메시지는 이모지를 적절히 섞어서 등)을 전부 조합해서 하나의 구조화된 프롬프트로 만들어 보냅니다. AI가 "적당히 좋은 말"이 아니라 "이 상황, 이 관계에 정확히 맞는 말"을 만들도록 유도하는 게 목적입니다.

**둘째, 응답 형식을 강제했습니다.** Gemini의 `responseSchema` 기능을 사용해서, AI가 항상 세 가지 안(정중형/감성형/단정형)을 정해진 JSON 구조로만 반환하도록 스키마를 걸어놨습니다. 이렇게 하면 AI 응답을 파싱하다가 형식이 틀어져서 화면이 깨지는 걸 원천적으로 막을 수 있습니다.

그리고 **API 키가 없어도 서비스가 죽지 않도록** 폴백 로직을 넣었습니다. `GEMINI_API_KEY`가 비어 있으면 서버가 미리 준비된 예시 문구 3개를 대신 반환합니다. 이건 단순한 예외 처리가 아니라, 데모나 발표 환경처럼 API 키 설정이 애매한 상황에서도 서비스가 정상적으로 동작하는 걸 보여드리기 위한 설계입니다.

---

## 5. 선택적 로그인/동기화 — Firebase (Authentication + Firestore)

로그인과 기기 간 동기화는 **Firebase**로 구현했는데, 여기서 저희가 중요하게 생각한 원칙이 있습니다. **"로그인은 완전히 선택 사항이어야 한다."**

기본적으로 관계 프로필과 생성 기록은 브라우저의 `localStorage`에 저장됩니다. 로그인을 하지 않아도 전체 기능이 아무 문제 없이 동작합니다. 다만 여러 기기에서 같은 관계 목록과 기록을 쓰고 싶은 사용자를 위해, **이메일 링크 기반의 비밀번호 없는 로그인**을 붙였고, 로그인하면 그 데이터가 Firestore로 동기화됩니다. 처음 로그인하는 순간에는 기존에 로컬에 저장돼 있던 데이터를 클라우드로 한 번 옮겨주는 마이그레이션 로직도 넣어놨습니다.

비밀번호 로그인 대신 이메일 링크 방식을 택한 이유는, 경조사 문구를 쓰는 이 서비스의 성격상 회원가입 절차 자체가 무겁게 느껴지면 안 된다고 판단했기 때문입니다. Firestore 보안 규칙도 본인 데이터만 접근할 수 있도록 별도로 작성해서 관리하고 있습니다.

---

## 6. 그 외 UX 디테일 — motion, lucide-react, html-to-image

마지막으로 완성도를 위해 붙인 라이브러리 세 가지입니다.

- **motion**: AI가 문구를 생성하는 동안이나 카드 디자인이 바뀔 때 자연스러운 전환 애니메이션을 넣는 데 사용했습니다.
- **lucide-react**: 아이콘을 이모지가 아니라 일관된 스타일의 SVG 아이콘 세트로 통일하기 위해 도입했습니다. 실제로 저희가 디자인을 다듬는 과정에서 이모지를 아이콘 대용으로 쓰던 부분들을 전부 이 라이브러리 아이콘으로 교체했습니다.
- **html-to-image**: 완성된 문구를 카드 형태의 이미지로 변환해서 PNG로 저장하거나 카카오톡에 바로 붙여넣을 수 있게 해주는 라이브러리입니다. 결국 사용자가 최종적으로 얻어가는 결과물이 "텍스트"가 아니라 "공유 가능한 카드 이미지"라는 점에서, 이 기능이 서비스의 실질적인 완성도를 좌우한다고 봤습니다.

---

## 마무리

정리하면, 저희는 **React·TypeScript·Vite·Tailwind로 빠르고 일관성 있게 화면을 만들고, Express 위에서 Gemini API를 구조화된 프롬프트와 스키마로 신뢰성 있게 호출하며, Firebase는 어디까지나 선택적인 보조 기능으로 붙이는** 방식으로 스택을 구성했습니다.

이 모든 선택의 기준은 결국 하나였습니다 — **누구나 로그인 없이 바로 써도, 경조사라는 상황에 어울리는 결과물을 안정적으로 받을 수 있어야 한다.** 저희 기술 스택은 이 목표를 위해 선택된 것입니다.

감사합니다.
