# 마음담음 — 기술 스택 Q&A 대비 노트

이 대화에서 실제로 오갔던 질문들을 정리한 것입니다. 발표 직전에 한 번 훑어보는 용도로 쓰세요. 모범 답변을 외우기보다, **"아, 이 흐름이었지"** 하고 떠올릴 수 있으면 충분합니다.

---

## A. 기술 스택 선정

**Q. 왜 이 기술 스택을 선택했나요?**
> 로그인 없이도 전체 기능이 동작해야 한다는 것, 경조사 상황에 어울리는 결과물을 안정적으로 내야 한다는 것 — 이 두 기준으로 골랐습니다. (각 기술별 이유는 발표 대본 참고)

**Q. 기능이 추가되면(페이지 분리, 회원가입) 기술 스택을 바꿔야 하나요?**
> 아니요. 페이지 분리는 React state나 react-router-dom 추가로, 회원가입은 이미 설치된 Firebase의 다른 기능(이메일+비밀번호 로그인)을 더 쓰는 것만으로 해결됩니다. 서버(`server.ts`)는 이미 모든 경로에서 index.html을 내려주도록 돼 있어서 라우팅 추가에도 손댈 필요가 없습니다.

---

## B. 클라이언트-서버 구조 / 계층

**Q. CS 구조가 뭔가요?**
> Client-Server 구조의 줄임말. 요청하는 쪽(클라이언트)과 처리하는 쪽(서버)을 나누는 기본 설계. 반대 개념은 독립형(standalone) 프로그램.

**Q. 3계층 구조를 설명해주세요.**
> CS 구조를 한 단계 더 세분화한 것. ① Client(React, 화면) → ② Server(Node.js+Express, 요청 처리) → ③ DB(Firebase, 데이터 저장).

**Q. 인증과 인가의 차이는?**
> 인증(Authentication) = "누구세요?" 신원 확인 → Firebase Auth 담당.
> 인가(Authorization) = "이거 해도 되나요?" 권한 확인 → `firestore.rules`의 `request.auth.uid == userId` 담당.
> 실제 코드 한 줄로 둘 다 보여줄 수 있음: `allow read, write: if request.auth != null && request.auth.uid == userId;`

**Q. 계층 수는 뭘 기준으로 세나요?**
> "코드를 어떻게 짜느냐"가 아니라 "물리적으로 어디서 실행되느냐". 브라우저(클라이언트)와 서버는 프로세스를 아무리 하나로 합쳐도 물리적으로 다른 곳이라, 계층 수는 줄지 않습니다.

---

## C. Node.js / Express / Vite

**Q. Node.js와 Express의 차이는?**
> Node.js = 자바스크립트를 서버에서 실행시켜주는 실행 환경(런타임). Express = 그 위에서 웹서버를 편하게 만들게 해주는 프레임워크.
> 비유: Node.js : Express = 파이썬 : Django.

**Q. Node.js는 운영체제인가요?**
> 아니요. 운영체제(Windows 등) 위에 설치해서 쓰는 프로그램 중 하나일 뿐입니다. Node.js를 지워도 OS는 멀쩡히 동작합니다.

**Q. Node.js가 서버를 직접 못 만드나요?**
> 만들 수 있습니다. Node.js 안에 `http`라는 내장 도구가 있어서 Express 없이도 서버를 짤 수 있어요. 다만 손이 많이 가서 Express라는 편의 도구를 씁니다.

**Q. Vite와 Express가 어떻게 하나로 통합되나요?**
> Vite를 `middlewareMode: true`로 설정하면 Vite가 자기 혼자 서버(포트)를 안 만들고, `app.use(vite.middlewares)`로 Express 안에 "미들웨어(부품)"로 들어옵니다. 그 결과 포트 하나(3000), 프로세스 하나로 통합됩니다.

**Q. 둘 중 하나를 다른 도구로 바꾸면 통합이 깨지나요?**
> 바뀐 도구가 "독립 서버 없이 미들웨어로만 동작하는 기능"을 갖고 있느냐에 달림. webpack은 `webpack-dev-middleware`로 가능. Next.js는 애초에 완제품 서버라 이 통합 방식 자체가 필요 없어짐.

**Q. 왜 이 프로젝트에서 Node.js를 썼나요?**
> ① 프론트엔드(TypeScript)와 언어를 통일해서 `types.ts` 같은 타입 정의를 서버(`server.ts`)에서도 그대로 재사용. ② Gemini SDK(`@google/genai`)가 Node.js(npm) 패키지. ③ Vite를 서버에 통합하려면 Vite와 같은 Node.js 생태계여야 함.

---

## D. Next.js와의 비교

**Q. Next.js로 바꾸면 뭐가 사라지고 뭐가 생기나요?**
> 사라짐: Express, Vite 설정. 생김: 폴더 기반 라우팅, API 라우트, `'use client'` 구분. 그대로: promptBuilder.ts, Firebase 로직, 타입 정의 등 핵심 로직.

**Q. Next.js가 Vite보다 무거운 도구인가요?**
> 네. Vite는 번들러 하나(부품)뿐이고, Next.js는 번들러+서버+라우팅+렌더링 전략까지 다 포함한 풀스택 프레임워크(완제품)입니다.

**Q. Next.js는 1계층인가요 2계층인가요?**
> 둘 다입니다. Client Component는 브라우저(1계층)에서, Server Component·API Route는 서버(2계층)에서 실행됩니다. Vite=1계층 전용, Express=2계층 전용, Next.js=1+2계층 동시 존재.

**Q. 프로젝트 크기가 커지면 Next.js로 가야 하나요?**
> 아니요, 기준은 "크기"가 아니라 "검색엔진(SEO) 노출이 필요한 공개 콘텐츠가 많은가"입니다. 마음담음처럼 로그인 없이 도구로 쓰는 서비스는 커져도 Vite+Express 조합이 유효합니다.

---

## E. Gemini API / 스키마

**Q. 스키마가 뭔가요?**
> 데이터가 어떤 구조(필드·타입)를 가져야 하는지 미리 정해둔 설계도. 엑셀 표의 "헤더 행"과 같은 개념.

**Q. 이 프로젝트에서 스키마는 어디에 쓰이나요?**
> `server.ts`의 Gemini `responseSchema` 한 곳. AI가 항상 `candidates` 배열(variantIndex/title/toneTag/content) 형태로만 답하도록 강제.

**Q. 데이터베이스 테이블은 있나요?**
> 전통적 테이블 기반 DB는 없습니다. 로그인 안 하면 localStorage(브라우저 내 임시 저장), 로그인하면 Firestore(컬렉션+문서 구조, "테이블/행/열" 대신 "컬렉션/문서/필드").

---

## F. 네트워크 기초

**Q. IP 주소, 포트, DHCP가 뭔가요?**
> IP = 인터넷에서 컴퓨터를 구분하는 주소. `127.0.0.1`(=localhost)은 "나 자신"을 가리키는 특수 주소.
> 포트 = 같은 컴퓨터 안 여러 프로그램을 구분하는 번호. 우리 서버는 3000번.
> DHCP = 공유기가 자동으로 IP를 나눠주는 방식.

**Q. 유동 IP가 뭔가요?**
> 고정해두지 않아서 바뀔 수 있는 IP. 같은 공유기에 계속 붙어있으면 실제로는 잘 안 바뀌지만, 다른 네트워크로 옮기면 반드시 바뀝니다. 노트북/데스크탑 둘 다 기본값은 유동 IP입니다.

---

## 발표·Q&A 임하는 자세 (마지막 조언)

- 모든 질문에 완벽하게 답할 필요 없습니다. 모르면 "그 부분은 확인 후 다시 말씀드리겠습니다"가 아는 척 틀리게 답하는 것보다 훨씬 낫습니다.
- 위 표에 있는 것만 소화해도, 이 프로젝트 규모 발표치고는 이미 상당히 깊이 있는 수준입니다.
- 질문이 예상 밖으로 오면, 이 노트에서 제일 가까운 개념(예: CS 구조, 인증/인가, 스키마)으로 연결해서 답변을 시작하면 자연스럽게 풀립니다.
