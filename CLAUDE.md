# CLAUDE.md — 마음담음 최종 기획 확정본

> 2026-08-21 기준. 이 문서가 **공식 계획**입니다.
> `WOOTAE1005/letter` (Next.js + Supabase)는 같은 아이디어의 초기 프로토타입이었으나 Supabase 연결·Gemini 키 문제로 미완성 상태에 머물렀습니다. 실제로 끝까지 완성된 이 프로젝트(`WOOTAE1005/Google`, 제품명 "마음담음")를 졸업작품으로 확정합니다. letter 저장소/폴더는 삭제하지 않고 그대로 두되, 더 이상 개발하지 않습니다.
> 아래 "확정 범위"는 실제 구현된 코드를 기준으로 정리했고, "데모 전 확정 To-do"와 "범위 밖(추후 과제)"을 명확히 나눴습니다.

---

## 1. 프로젝트 정체성

- **제품명**: 마음담음
- **한 줄 정의**: 관계·상황·톤을 고르면 AI가 경조사 문구(봉투문구/문자/카톡메시지) 또는 일반 편지(사과/감사/응원/그리움/안부)를 대신 지어주는 웹 서비스
- **개발**: 1인 개발 (이우태, 동서울대학교 컴퓨터소프트웨어학과) — 기획·디자인·프론트엔드·백엔드·AI 프롬프트 엔지니어링 전 과정 직접 수행
- **로컬 경로**: `C:\Users\dldnx\OneDrive\Desktop\졸업작품\마음담음`
- **저장소**: https://github.com/WOOTAE1005/Google (public)

---

## 2. 기술 스택 (확정, 변경 없음)

| 영역 | 선택 | 비고 |
| --- | --- | --- |
| Frontend | React 19 + TypeScript + Vite 6 | Express 미들웨어 모드로 백엔드와 통합 |
| Styling | Tailwind CSS v4 | `--color-brand-*` 토큰 = "장미빛 잉크" 팔레트 |
| Backend | Express | `/api/generate-message` 단일 엔드포인트 |
| AI | `@google/genai` (Gemini) | `responseSchema`로 JSON 3안 강제, 키 없으면 폴백 |
| 인증/동기화 (선택) | Firebase Auth(이메일 링크) + Firestore | 미설정 시 localStorage로 전체 기능 동일하게 동작 |
| 기타 | `motion`, `lucide-react`/`react-icons`, `html-to-image` | 애니메이션, 아이콘, 카드 이미지 내보내기 |

---

## 3. 확정된 핵심 기능 (구현 완료, 데모 대상)

- [x] 관계 프로필 관리 (관계 유형·친밀도·톤·기억 노트 저장·재사용)
- [x] 경조사 조건부 상황 선택 (경사/조사 → 세부 항목 → 상황 태그)
- [x] **일반 편지 모드** (2026-08-21 추가) — 사과/감사/응원/그리움/안부, 경조사와 같은 인프라 재사용
- [x] Gemini 기반 3안 동시 생성 (정중형/감성형/단정형) + 톤 조율 재생성
- [x] 전달용 카드 — 레이아웃 5종 × 색상 팔레트 13종 (신규 "장미빛 잉크" 포함), 이미지 저장/클립보드 복사
- [x] 예법 가이드 — 관계·상황별 금기 표현 안내
- [x] 생성 기록 보관 (로그인 시 Firestore, 아니면 localStorage)
- [x] 선택적 로그인 동기화 (이메일 링크, 미설정 시 전체 기능 정상 동작)
- [x] 사이트 전역 "장미빛 잉크" 디자인 시스템 (2026-08-21 리스킨 완료)
- [x] API 키 없을 때도 서비스가 죽지 않는 폴백 로직

**이 목록은 잠근다.** 데모 전까지 이 기능들의 동작 안정성만 지키고, 새 기능을 추가하지 않는다.

---

## 4. 데모 전 확정 To-do (지금부터 이것만 한다)

1. [ ] **실제 Gemini API 키 발급 및 적용** — 현재 `.env`의 `GEMINI_API_KEY`가 비어 있어 항상 폴백 문구만 나옴. AI Studio에서 **새 프로젝트로 키 생성**해서 `.env`에 반영하고, 실제 생성 결과로 최종 리허설.
2. [x] **카카오톡 실공유 연동 — 완료 (2026-08-24)** — 실제 Kakao JS 키 발급 + `http://localhost:3000` 도메인 등록 완료, `.env`에 반영. 로컬에서 `window.Kakao.isInitialized() === true` 확인, `Share.sendDefault()` 호출까지 에러 없이 실행됨. 배포 시에는 실제 배포 도메인도 Kakao Developers 플랫폼에 추가로 등록해야 함.
3. [x] **리허설 1회 — 구조적 완료 (2026-08-24)** — 경조사 모드(조사/부고/세부태그/문자) + 일반편지 모드(응원편지/카톡메시지) 각각 관계 선택 → 생성 → 카드 → 카카오 공유 버튼까지 전체 플로우 확인, 콘솔 에러 없음, 생성 기록 정상 저장 확인. 리허설 중 실제 버그(조사 폴백 문구의 "차장님님" 존칭 중복)를 발견해 수정함. **단, 지금은 폴백 텍스트로 확인한 것이라 — Gemini 키를 넣은 뒤 실제 AI 응답으로 한 번 더 돌려보는 게 안전함.**

이 3가지 외에는 데모 전에 추가로 손대지 않는다.

---

## 5. 범위 밖 (데모 이후 과제로 명확히 보류)

아래는 아이디어로는 유효하지만, **지금은 하지 않는다** — 발표 자료(로드맵 슬라이드)에는 "향후 계획"으로만 언급.

- Cloud Run/AI Studio 정식 배포 (로컬 데모로 충분, 배포는 여유 있을 때)
- Firebase 로그인 실제 활성화 (Authentication/Firestore 콘솔 설정) — 로그인 없이도 전체 기능이 동작하므로 데모 필수 아님
- 편지 주제 확장 (축하 편지, 이별 편지 등 카테고리 추가)
- 생성 이력 기반 프롬프트 품질 고도화
- UI 추가 커스터마이징 (카드 레이아웃/팔레트 추가 등)
- **Next.js 전환 (1년 유지보수를 실제로 하게 될 경우에 한해)** — 2026-08-21 논의 확정. 현재 스택(React 19 + Express + Vite 미들웨어 통합)은 단기 캡스톤 데모엔 적합하지만, 실서비스로 1년 이상 운영한다면 Next.js가 더 나은 선택이라고 판단함(표준 프레임워크라 문서/커뮤니티가 풍부, API 라우트 내장으로 지금의 Express+Vite 커스텀 통합이 사라짐, SSR 지원으로 OG 메타태그/링크 미리보기 문제가 구조적으로 해결됨, Vercel 배포 생태계가 지금의 AI Studio→Cloud Run 경로보다 성숙함).
  - **작업 위치**: `WOOTAE1005/letter` 폴더(`C:\Users\dldnx\OneDrive\Desktop\letter`)에서 진행 — 마음담음 폴더/저장소와는 완전히 분리돼 있어 작업이 마음담음에 영향을 줄 수 없음.
  - **재활용 방향**: letter 프로젝트가 이미 만들어둔 Next.js+Supabase 골격(관계 데이터 모델, `promptBuilder.ts` 구조, 컴포넌트 뼈대)을 시작점으로 삼되, 내용은 마음담음에서 실제로 검증된 로직(프롬프트 구조, 키워드 트리, 예법 주의사항 등)을 기준으로 다시 채운다.
  - **버전 다운그레이드(React 18/Tailwind v3)는 하지 않는다** — 지금 시점엔 React 19/Tailwind v4가 더 이상 "막 나온 버전"이 아니라 다운그레이드의 마이그레이션 비용이 실익보다 큼. 대신 정기적으로 최신 패치를 따라가는 일반 유지보수로 대응한다.
  - 착수 시점: 데모가 끝나고, 마음담음 서비스를 실제로 계속 운영하기로 결정한 뒤 — 별도의 새 기획 사이클로 진행.

---

## 6. 데이터 모델 (참고, 변경 없음)

`src/types.ts` 기준 — `Relationship`, `PromptKeyword`(category: `경사`/`조사`/`편지`), `MessageCandidate`, `GeneratedMessageRecord`. 새 카테고리(예: 편지 주제)는 `src/lib/keywords.ts`에 데이터만 추가하면 확장되는 구조를 유지한다 (코드 변경 없이 확장 — 일반편지 모드가 이미 이 방식으로 만들어짐).

---

## 7. 발표 자료

`마음담음_발표자료.pptx` (PSST 구조, 15슬라이드) 준비 완료 — 실제 git 커밋 기록 기반 이력/일정, 장미빛 잉크 팔레트 적용. 섹션 4의 To-do가 끝나면 이 발표자료의 로드맵 슬라이드와 내용이 그대로 맞아떨어진다.
